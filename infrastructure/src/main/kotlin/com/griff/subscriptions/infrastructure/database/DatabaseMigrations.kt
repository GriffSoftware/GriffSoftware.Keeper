package com.griff.subscriptions.infrastructure.database

import androidx.room.migration.Migration

/**
 * All migrations applied to [SubscriptionDatabase], in order.
 *
 * Empty while the schema is at version 1; new entries are added here together with the exported
 * schema of the new version.
 */
internal val DatabaseMigrations: Array<Migration> = emptyArray()
