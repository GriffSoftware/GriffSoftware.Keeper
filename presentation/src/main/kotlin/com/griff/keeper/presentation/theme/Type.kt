package com.griff.keeper.presentation.theme

import androidx.compose.material3.Typography

/**
 * Material 3 default typography.
 *
 * The app deliberately keeps the platform type scale; overriding it here would only be justified by
 * a custom brand font.
 */
internal val AppTypography = Typography()
